#ifndef LCS_HPP
#define LCS_HPP

#include <vector>

#include "table.hpp"

namespace lcs {

std::vector<char> find(const std::vector<char>& S1, const std::vector<char>& S2);

}  // namespace lcs


#endif  // LCS_HPP
